# Auto-deploy to IntelWebHost / cPanel via FTP when you push to main.
#
# Enable this workflow (default): GitHub → Settings → Secrets and variables → Variables
#   DEPLOY_TARGET = ftp   (or leave unset)
#
# GitHub repo → Settings → Secrets → Actions:
#   FTP_HOST         — rakib07.com (or ftp.rakib07.com)
#   FTP_USERNAME     — your cPanel username
#   FTP_PASSWORD     — your cPanel / FTP password
#   FTP_SERVER_DIR   — remote folder, e.g. /rrpdreaminn/
#   DATABASE_URL     — production MySQL (remote hostname, NOT localhost)
#                      e.g. mysql://user:pass@server123.yourhost.com:3306/dbname
#
# One-time on cPanel (critical — without this you get 404):
#   1. FTP_SERVER_DIR must match the Node.js "Application root" exactly
#      e.g. /home/USERNAME/rrpdreaminn/  (trailing slash required)
#   2. Setup Node.js App → Application startup file: server.js
#   3. Application URL must be your domain (e.g. rakib07.com)
#   4. Add env vars: NODE_ENV=production, DATABASE_URL, NEXTAUTH_SECRET, NEXTAUTH_URL
#   5. Click "Restart" in Node.js panel after the first deploy
#   6. Remote MySQL → add host "%" so GitHub Actions can reach your database

name: Deploy to cPanel (FTP)

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: true

jobs:
  deploy:
    if: ${{ !vars.DEPLOY_TARGET || vars.DEPLOY_TARGET == 'ftp' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Sync database schema to server MySQL
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          if [ -z "$DATABASE_URL" ]; then
            echo "::error::Missing DATABASE_URL secret. Add your production MySQL URL in GitHub → Settings → Secrets → Actions"
            exit 1
          fi
          npx prisma db push --skip-generate --accept-data-loss

      - name: Build app
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          npx prisma generate
          npm run build
          node scripts/verify-standalone-build.js
          mkdir -p .next/standalone/tmp
          date -u +"%Y-%m-%dT%H:%M:%SZ" > .next/standalone/tmp/restart.txt

      - name: Upload via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          protocol: ftps
          local-dir: ./.next/standalone/
          server-dir: ${{ secrets.FTP_SERVER_DIR }}
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.git*/**
            **/.env
            **/.env.*
            **/.github/**
            **/.htaccess
            **/.well-known/**
